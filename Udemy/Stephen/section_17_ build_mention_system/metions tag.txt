
-- here we can able to mention the user in photo and conternt of the post 





table users{
id serial [pk,increment]
created_at timestamp
updated_at timestamp
username varchar(40)

}

table posts{
  id serial [pk,increment]
  created_at timestamp
  updated_at timestamp
  url varchar(200)
  user_id integer [ref:> users.id]
  caption varchar(240)
  lat real
  lang real
}

table comments{
id serial [pk,increment]
created_at timestamp
updated_at timestamp
contents varchar(240)
post_id integer [ref: >posts.id]
user_id integer [ref:>users.id]
}

table likes{
  id serial [pk , increment]
  created_at timestamp

  post_id integer [ref:>posts.id]
  comment_id integer [ref:> comments.id]
   user_id integer [ref:> users.id]
}

table photo_tag{
  id serial [pk, increment]
  created_at timestamp
  updated_at timestamp
  post_id integer [ref:>users.id]
  user_id interger [ref:>users.id]
  x integer
  y integer
}

table captions_tag{
  id serial [pk , increment]
  created_at timestamp
  post_id integer [ref: >posts.id]
  user_id integer [ref: >users.id]
}