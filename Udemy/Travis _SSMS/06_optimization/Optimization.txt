1) Filter the data as earlier as possible before joining the table
2) avoid lot of join in single select query

if we join the table then database need to fully scan the both table to join because it will make sure their is not join left
but we use update then 